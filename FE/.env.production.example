# Production Environment Variables Template
# Copy this file to .env.production and fill in your actual values

# Database Configuration (Required)
# Get these from your cloud database provider (PlanetScale, Railway, etc.)
DB_HOST=your-database-host.com
DB_USER=your-database-username
DB_PASSWORD=your-database-password
DB_NAME=syllabus_calendar
DB_PORT=3306

# JWT Secret (Required)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here-min-32-characters

# Backend API URL (Required if backend is deployed separately)
# Leave as http://localhost:3001 for local development
# Update to your backend Vercel URL for production
BACKEND_API_URL=http://localhost:3001

# Optional: Clova AI Services (if using AI features)
# CLOVA_STUDIO_API_KEY=your-clova-studio-api-key
# CLOVA_STUDIO_URL=your-clova-studio-url
