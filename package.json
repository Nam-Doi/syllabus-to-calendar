{
  "name": "syllabus-to-calendar",
  "version": "1.0.0",
  "description": "Convert course syllabi into organized week-by-week calendar plans",
  "private": true,
  "scripts": {
    "dev": "npm run dev:fe",
    "dev:be": "cd BE && npm run dev",
    "dev:fe": "cd FE && npm run dev",
    "dev:all": "npm-run-all --parallel dev:be dev:fe",
    "install:all": "npm install && cd BE && npm install && cd ../FE && npm install",
    "build": "cd FE && npm run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}

